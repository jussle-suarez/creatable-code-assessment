const BasePage = require('./base.page.js');

class ContentPage extends BasePage {
    constructor(page) {
        super(page);
        // declare all locators from content page here.
    }
}

module.exports = ContentPage;